export interface LendPosition {
  id: string;
  duration: number;
  tokenId: string;
  price: number;
  blockScanUrl: string;
}

export interface BorrowPosition {
  id: string;
  duration: number;
  tokenId: string;
  price: number;
  blockScanUrl: string;
}
